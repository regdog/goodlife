<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>GoodLife</title>
  <%= stylesheet_link_tag :all %>
  <link type="text/css" rel="stylesheet" href="http://fonts.googleapis.com/css?family=Droid+Sans:400,700">
  <%= javascript_include_tag :defaults, 'jquery-ui-dialog', 'script', 'jquery.pageless.min' %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div id="container">
    <div id="header">
      <div class="logo"><%= link_to image_tag("/images/im_logo_full.png"), root_path %></div>
      <%= render :partial => "shared/navigation" %>
    </div>

    <% if content_for?(:page_header) %>
      <%= yield :page_header %>
    <% end %>

    <div class="content row">
      <%= yield %>
    </div>
    <div id="footer">
      <%= render :partial => "shared/footer" %>
    </div>
  </div>
</body>
</html>
