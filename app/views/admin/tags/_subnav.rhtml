<ul class="navigation primary">
  <% Tag::KINDS.each do |type| %>
    <li class="<%= active_li_css(@view_by, type) %>">
			<%= 
				link_to_unless(
				  (@view_by == type),
					type,
					:controller => 'admin/tags',
					:action => 'index',
					:type => type
				)
			%>
		</li>
	<% end %>
</ul>
<div class="clear"></div>