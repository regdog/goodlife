<ul class="navigation primary">
  <% FeatCategory.all.each do |type| %>
    <li class="<%= active_li_css(@view_by, type) %>">
			<%= 
				link_to_unless(
				  (@view_by == type),
					type.name,
					:controller => 'admin/feats',
					:action => 'index',
					:type => type
				)
			%>
		</li>
	<% end %>
</ul>
<div class="clear"></div>