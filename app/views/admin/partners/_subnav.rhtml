<ul class="navigation primary">
  <% Partner::TYPES.each do |type| %>
    <li class="<%= active_li_css(@view_by, type) %>">
			<%= 
				link_to_unless(
				  (@view_by == type),
					type.pluralize,
					:controller => 'admin/partners',
					:action => 'list',
					:type => type
				)
			%>
		</li>
	<% end %>
</ul>
<div class="clear"></div>