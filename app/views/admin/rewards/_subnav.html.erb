<ul class="navigation primary">
  <% RewardCategory.all.each do |type| %>
    <li class="<%= active_li_css(@view_by, type) %>">
			<%= 
				link_to_unless(
				  (@view_by == type),
					type.name,
					:controller => 'admin/rewards',
					:action => 'index',
					:type => type
				)
			%>
		</li>
	<% end %>
</ul>
<div class="clear"></div>