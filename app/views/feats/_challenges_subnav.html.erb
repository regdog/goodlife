<ul class="navigation primary">
  <% Challenge::SORT_TYPES.each do |type| %>
    <li class="<%= active_li_css(@view_by, type) %>">
			<small><%=
				link_to_unless(
				  (@view_by == type),
					type,
					:controller => 'feats',
					:action => 'challenges',
					:type => type
				)
			%></small>
		</li>
	<% end %>
</ul>
<div class="clear"></div>