$('#checkin_<%= @checkin.id %>_props').html('<%= escape_javascript(render(:partial => "shared/comment", :locals => { :checkin => @checkin }))%>').hide().fadeIn('slow');
  // auto expand textarea
  $('textarea').focus( function () {
    $(this).animate({ height: "40px" });
    $(this).next('.giveprop').show();
    return false;
  });